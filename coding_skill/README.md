# C언어 디자인 패턴 학습 자료

## 📚 개요

이 폴더는 C언어에서 **Wrapper 패턴**과 **Adapter 패턴**을 학습하기 위한 예제 코드를 포함하고 있습니다.

## 📂 파일 구성

### 1. `01_wrapper_pattern.c` - Wrapper 패턴
**Wrapper 패턴**은 기존 함수나 라이브러리를 감싸서(wrap) 추가 기능을 제공하는 디자인 패턴입니다.

**주요 특징:**
- 원본 함수의 기능은 유지하면서 전/후처리 로직 추가
- 로깅, 에러 처리, 성능 측정 등에 활용
- 기존 코드를 수정하지 않고 기능 확장 가능

**예제 내용:**
- `fopen`, `fclose`의 로깅 래퍼
- `malloc`, `free`의 메모리 추적 래퍼
- 에러 처리 및 NULL 체크 자동화

**컴파일 및 실행:**
```bash
gcc -o 01_wrapper 01_wrapper_pattern.c
./01_wrapper
```

---

### 2. `02_adapter_pattern.c` - Adapter 패턴
**Adapter 패턴**은 호환되지 않는 인터페이스를 가진 두 개의 구성 요소를 연결하는 디자인 패턴입니다.

**주요 특징:**
- 서로 다른 인터페이스를 통일된 방식으로 사용
- 레거시 시스템과 신규 시스템 통합에 유용
- "중간 변환기" 역할 수행

**예제 내용:**
- 레거시 센서 (화씨 온도, 다른 함수명)
- 신규 센서 (켈빈 온도, 다른 에러 처리)
- Adapter를 통한 통일된 인터페이스 제공
- 모든 센서를 동일한 방식으로 사용

**컴파일 및 실행:**
```bash
gcc -o 02_adapter 02_adapter_pattern.c
./02_adapter
```

---

### 3. `03_wrapper_adapter_combined.c` - 통합 예제
**Wrapper + Adapter 패턴**을 함께 사용한 실전 예제입니다.

**시스템 구조:**
```
[다양한 센서들]
       ↓
  [Adapter 레이어] ← 인터페이스 통일
       ↓
  [Wrapper 레이어] ← 로깅, 검증, 에러 처리
       ↓
[모니터링 시스템]
```

**예제 내용:**
- 3가지 다른 센서 (온도, 습도, 압력)
- Adapter로 인터페이스 통일
- Wrapper로 로깅 및 데이터 검증 추가
- 실시간 센서 모니터링 시스템 구현

**컴파일 및 실행:**
```bash
gcc -o 03_combined 03_wrapper_adapter_combined.c -lm
./03_combined
```

---

## 🎯 학습 순서

### 초급 학습자
1. `01_wrapper_pattern.c` 읽고 실행
2. `02_adapter_pattern.c` 읽고 실행
3. 각 패턴의 개념과 차이점 이해
4. `03_wrapper_adapter_combined.c`로 통합 학습

### 중급 학습자
1. 각 예제 코드를 수정하여 새로운 기능 추가
2. 자신만의 센서나 장치로 예제 재작성
3. 에러 처리 케이스 추가
4. 성능 측정 기능 추가 (시간, 메모리 등)

### 고급 학습자
1. 다른 디자인 패턴과 조합 (Factory, Observer 등)
2. 멀티스레드 환경에서의 안전성 고려
3. 실제 하드웨어 (GPIO, I2C, SPI 등)와 연동
4. 플러그인 시스템 구현

---

## 💡 패턴 비교

| 특성 | Wrapper 패턴 | Adapter 패턴 |
|------|-------------|-------------|
| **목적** | 기능 추가 | 인터페이스 변환 |
| **대상** | 단일 함수/라이브러리 | 서로 다른 시스템 |
| **변경점** | 기능 확장 | 호환성 제공 |
| **사용 시기** | 로깅, 검증, 에러 처리 필요시 | 다양한 인터페이스 통합 필요시 |

---

## 🔧 실무 적용 사례

### Wrapper 패턴
- **임베디드 시스템**: HAL 함수 로깅
- **메모리 관리**: 메모리 누수 추적
- **성능 모니터링**: 함수 실행 시간 측정
- **테스트**: Mock 객체 생성

### Adapter 패턴
- **하드웨어 통합**: 다양한 센서 통합
- **프로토콜 변환**: UART, SPI, I2C 통일
- **레거시 통합**: 기존 시스템과 신규 시스템 연동
- **멀티 벤더**: 서로 다른 제조사 장비 통합

### Wrapper + Adapter 통합
- **IoT 플랫폼**: 다양한 디바이스 통합 관리
- **산업 자동화**: 센서 모니터링 시스템
- **테스트 프레임워크**: 통일된 테스트 인터페이스
- **플러그인 시스템**: 확장 가능한 아키텍처

---

## 📝 학습 체크리스트

### Wrapper 패턴 이해
- [ ] Wrapper 패턴의 개념 이해
- [ ] 원본 함수와 래퍼 함수의 관계 파악
- [ ] 로깅 기능 구현 방법 학습
- [ ] 에러 처리 추가 방법 이해
- [ ] 자신만의 래퍼 함수 작성

### Adapter 패턴 이해
- [ ] Adapter 패턴의 개념 이해
- [ ] 인터페이스 통일의 필요성 이해
- [ ] 함수 포인터 활용 방법 학습
- [ ] 다양한 데이터 형식 변환 방법 파악
- [ ] 자신만의 어댑터 구현

### 통합 적용
- [ ] 두 패턴의 차이점 명확히 구분
- [ ] 적용 순서의 중요성 이해
- [ ] 실전 프로젝트에 적용 가능 여부 판단
- [ ] 코드 구조와 아키텍처 이해
- [ ] 확장 가능한 시스템 설계 능력 향상

---

## 🚀 추가 학습 자료

### 관련 개념
- **디자인 패턴**: Factory, Singleton, Observer
- **함수 포인터**: 콜백, 이벤트 핸들러
- **구조체**: 캡슐화, 모듈화
- **메모리 관리**: 동적 할당, 해제

### 연습 과제
1. 파일 I/O에 버퍼링 기능을 추가한 래퍼 작성
2. UART, SPI 통신을 통일된 인터페이스로 만드는 어댑터 구현
3. 로깅 레벨을 런타임에 변경할 수 있는 시스템 구현
4. 여러 센서의 평균값을 계산하는 모니터링 시스템 확장

---

## 📖 참고 문헌

- Design Patterns: Elements of Reusable Object-Oriented Software (GoF)
- Embedded C Programming: Design Patterns for Resource-Constrained Systems
- C 언어로 배우는 디자인 패턴

---

## ⚠️ 주의사항

- 예제 코드는 학습 목적으로 작성되었습니다
- 실제 프로젝트에 적용 시 에러 처리를 더욱 강화하세요
- 메모리 관리에 주의하세요 (누수, 이중 해제 등)
- 멀티스레드 환경에서는 동기화를 고려하세요

---

## 📧 문의 및 피드백

이 학습 자료에 대한 질문이나 개선 사항이 있다면 언제든지 문의해주세요!

**Happy Coding! 🎉**
